# Only needed for post-processing on your workstation (scripts/plot_results.py).
# The measurement container itself (Dockerfile.measure) has no Python
# dependencies beyond python3-yaml, installed via apt.
matplotlib>=3.8
