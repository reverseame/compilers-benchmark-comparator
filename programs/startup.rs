// Empty workload: measures process startup and teardown only (dynamic
// linking, relocation processing, language-runtime initialization).
// Companion of startup.cpp.
fn main() {
    println!("ok");
}
